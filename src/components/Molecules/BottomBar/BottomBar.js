import styled from "styled-components";

const StyledWrapper = styled.div`
  background-color: ${({ theme }) => theme.backgroundColor.primaryDark};
  height: 15vh;
  border-radius: 10px;
`;

const BottomBar = () => {};

export default BottomBar;
